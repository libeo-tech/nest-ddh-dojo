enum ColorEnum {
  RED
  GREEN
  BLUE
  YELLOW
  WHITE
  BLACK
}

type Dragon {
  id: ID!
  level: Int!
  color: ColorEnum
}

type Mutation {
  generateRandomDragon: Boolean!
}

type Query {
  getAllDragons: [Dragon!]!
}
