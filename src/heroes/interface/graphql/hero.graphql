type Hero {
  id: ID!
  name: String!
  xp: Int!
  level: Int!
  inventory: [Item]
}

type Mutation {
  createHero(name: String!): Boolean!
  attackDragon(heroId: ID!, dragonId: ID!): Boolean!
}

type Query {
  getHero(id: ID!): Hero!
}
